import type { NextPage } from 'next';
import Nav from '@components/Nav';
import Link from 'next/link';

const Home: NextPage = () => {
	return (
		<div>
			<Nav />
		</div>
	);
};

export default Home;
