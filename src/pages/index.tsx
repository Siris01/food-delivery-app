import Nav from '@components/Nav';
import type { NextPage } from 'next';
import Link from 'next/link';

const Home: NextPage = () => {
	return (
		<div>
			hello
		</div>
	);
};

export default Home;
